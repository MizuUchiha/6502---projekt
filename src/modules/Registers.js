export  class Registers {
    constructor() {
        this.A = 0; // Akumulator
        this.X = 0; // Rejestr indeksowy X
        this.Y = 0; // Rejestr indeksowy Y
        this.PC = 0; // Licznik programu
        this.SP = 0; // Wskaźnik stosu
        this.P = 0; // Rejestr flag
    }

    // Metody do manipulacji rejestrami
}